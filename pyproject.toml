[project]
name = "codex"
version = "1.0.0"
description = "A data dictionary for Adobe Analytics report suites"
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.11.2",
    "flask>=3.1.2",
    "pyyaml>=6.0",
    "requests>=2.32.5",
]
