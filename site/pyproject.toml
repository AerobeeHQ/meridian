[project]
name = "codex-site"
version = "1.0.0"
description = "Codex brochure site — built with staticninja"
requires-python = ">=3.11"
dependencies = [
    "staticjinja>=5.0.0",
    "jinja2>=3.1.0",
]

[tool.uv]
dev-dependencies = []
